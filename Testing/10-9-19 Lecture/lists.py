# A sequence is an object that holds multiple items of data ,
# Stored one after the other.
# A list is an object that contains multiple data items. List is a sequence.
# Lists are mutable, which means that their contents can be changed during
# the runtime. 
# List is a dynamic data structure
# This means list can shrink and grow. 
# WE can use indexing, slicing, and various methods to work with the list.
names = ['John', 'David', 'Sarah']
# We can store different data types with list.
info = ['Alicia', 27, 1550.87]
num = list(range(1,10,2))
print(num)
# The repetition operator is (*)
""" list * N """
numbers = [0]*5
print(numbers)