numbers =[1,2,3,4,5]
print(numbers[1:3])
print(numbers[:3])
print(numbers[:])
# Number 3 is not included
# Python uses the length of the list as the end
# If we leave out a start and end, we will get all results.